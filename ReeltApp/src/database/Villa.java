package database;

public class Villa extends DataBase {
    public Villa(Integer room_count, Integer floor, Float area, Location location, Offer offer) {
        super(room_count, floor, area, location, offer);
    }
}
